syntax = "proto3";

// namespace
package remoteps;

// RemotePs service definition.
service RemotePs {
}

// general information message of a process
message Process {
	string 	name = 1;
	uint32 	pid  = 2;
	string 	exe  = 3;
}
